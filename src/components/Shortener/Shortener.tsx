import Container from '../Container';
import './Shortener.styles.css';

const Shortener = () => {
  return (
    <section>
      <Container>Shortener</Container>
    </section>
  );
};

export default Shortener;
